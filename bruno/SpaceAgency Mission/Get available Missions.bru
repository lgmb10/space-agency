meta {
  name: Get available Missions
  type: http
  seq: 10
}

get {
  url: http://localhost:8080/api/missions/available
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth-token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
