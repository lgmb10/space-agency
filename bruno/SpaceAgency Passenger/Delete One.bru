meta {
  name: Delete One
  type: http
  seq: 5
}

delete {
  url: http://localhost:8080/api/passengers/16
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth-token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
